<h1>Editing Vitamin</h1>

<%= link_to 'Show', @vitamin %> |
<%= link_to "Delete vitamin", vitamin_path(@vitamin.id), method: :delete, data: { confirm: 'Are you sure?' }%> |
<%= link_to 'Back', vitamins_path %>

<%= render 'form', vitamin: @vitamin %>
