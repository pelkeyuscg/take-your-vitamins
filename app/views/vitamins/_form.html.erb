<%= form_with(model: vitamin) do |form| %>
  <% if vitamin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo.errors.count, "error") %> prohibited this vitamin from being saved:</h2>

      <ul>
        <% vitamin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :amount_per_serving %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
